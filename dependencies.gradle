/*
 * 1.20.1 dependencies — Forge 47.3.0 + FG6 + mojmap + JDK17
 * Minimal build: only minecraft/forge + JEI. Others are commented out (回答④).
 * GTNH's rfg.deobf / devOnlyNonPublishable are removed; use fg.deobf for obfuscated mods.
 */

dependencies {
    // Core: Forge 1.20.1-47.3.0 (回答① 47.3)
    minecraft 'net.minecraftforge:forge:1.20.1-47.3.0'

    // JEI — kept (回答③). Located at https://maven.blamejared.com (mezz.jei). 15.20.0.109 verified 2026-08
    compileOnly fg.deobf("mezz.jei:jei-1.20.1-common-api:15.20.0.109")
    runtimeOnly fg.deobf("mezz.jei:jei-1.20.1-forge:15.20.0.109")

    // --- Commented out for minimal build (回答④): uncomment if 1.20.1 coordinate verified ---

    // IC2 (EU API BasicSink/BasicSource) — 1.20.1 version exists, verify latest
    // compileOnly fg.deobf("net.industrial-craft:industrialcraft-2:2.8.222-ex112") { transitive = false }

    // Forestry — 1.20.1 forge variant semi-maintained, verify
    // compileOnly fg.deobf("curse.maven:forestry-...:xxxx") { transitive = false }

    // BuildCraft — community maintained, fuel registry only
    // compileOnly fg.deobf("curse.maven:buildcraft-...:xxxx") { transitive = false }

    // Railcraft Reborn variant — verify / omit-leaning
    // compileOnly fg.deobf("curse.maven:railcraft-...:xxxx") { transitive = false }

    // Biomes O' Plenty — active 1.20.1
    // compileOnly fg.deobf("curse.maven:biomes-o-plenty-220318:5806419") { transitive = false }

    // Thermal Series (TE4 successor) — IMC only, low impact
    // compileOnly fg.deobf("curse.maven:thermal-api-...:xxxx") { transitive = false }

    // Bamboo — 1.20.1 version exists, keep LoadBambooPlugin, verify API (CookingRegistry/GrindRegistry)
    // compileOnly fg.deobf("curse.maven:bamboo-...:xxxx") { transitive = false }
    // or Modrinth: maven(modrinth) + "maven.modrinth:bamboo:xxxx"

    // --- Removed (1.20.1 incompatible / Omit) ---
    // Thaumcraft (1.12.2止まり) — removed
    // NEI/CodeChickenCore/AppleCore/MCEconomy2/SextiarySector2/CraftGuide/CoFH RF/PPC/Tofu/Wa etc. — removed
    // EndlessIDs/fplib/chunkapi — removed (1.20.1 Holder/RL handles it)
    // Local jars Bamboo-3.0.0.9.jar / wa_1.7.10.10_1180.jar — removed (not on 1.20.1)
}
